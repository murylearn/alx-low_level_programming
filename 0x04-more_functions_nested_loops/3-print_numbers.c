#include "main.h"

/**
 * print_numbers - prints the numbers, from 0 t0 9, followed by a new line.
 * Return: none
 */

void print_numbers(void)
{
	for (int i = 48; i < 58; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
